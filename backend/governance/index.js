'use strict';const{createGovernedRouter}=require('./router');const{pool}=require('../db');const auth=require('../middleware/auth');const{evaluate}=require('./domain');module.exports=createGovernedRouter({pool,auth,evaluate,workflowType:'privacy-operations',providers:['data-catalog','warehouse','saas','iam','ticketing','consent','legal-hold','deletion-export'],approverRoles:['privacy_officer','dpo','legal','admin']});
